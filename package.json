{
  "name": "self-healing-cicd-poc",
  "version": "1.0.0",
  "description": "POC: Self-Healing CI/CD Pipeline with AI-Powered GitHub Actions using GitHub Models (free tier)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --verbose --coverage",
    "lint": "eslint src/ __tests__/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "src/**/*.js"
    ]
  }
}
